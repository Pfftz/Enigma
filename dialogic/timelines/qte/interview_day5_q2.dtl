join mega_interviewer center

kamu (a): Would you sacrifice work-life balance for company success?

[signal arg="timer_start"]

- Absolutely, company success is my priority
	set {interview_score} += 25
	set {interview_health} += 25
	[signal arg="timer_end"]
	kamu (senang): Excellent! True corporate dedication. That's executive material.
	[signal arg="correct_choice"]

- I'll work hard but need some personal time
	set {interview_score} += 5
	set {interview_health} += 5
	[signal arg="timer_end"]
	kamu (senang): Compromise shows weakness. We need total commitment.
	[signal arg="correct_choice"]

- Work-life balance is important for productivity
	set {interview_score} -= 8
	set {interview_health} -= 8
	[signal arg="timer_end"]
	kamu (a): Productivity comes from dedication, not comfort.
	[signal arg="wrong_choice"]

- No, personal life comes first
	set {interview_score} -= 20
	set {interview_health} -= 20
	[signal arg="timer_end"]
	kamu (a): Wrong answer. MegaCorp demands total loyalty.
	[signal arg="wrong_choice"]

mega_interviewer: Time for the ultimate performance test
[signal arg="start_bubble_minigame"]
